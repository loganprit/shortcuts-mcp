{
  "name": "shortcuts-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "format": "biome format .",
    "format:check": "biome format --check .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "bplist-parser": "^0.3.2",
    "plist": "^3.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "bun-types": "^1.1.8",
    "typescript": "^5.4.5"
  }
}
